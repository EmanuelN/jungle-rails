<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Order #<%= @order.id %></h1>
    <p>
      Thank you for your order, <%= User.where(email: @order.email).first.first_name %>
    </p>
    <% @order.line_items.each do |item| %>
    <p>Item: <%= item.product.name %>
    Unit Price: <%= (item.item_price_cents).to_i / 100.0 %>
    Qty: <%= item.quantity %>
    Price: <%= (item.total_price_cents).to_i / 100.0 %></p>
    <% end %>
    <p>Total: <%= @order.total_cents.to_i / 100.0 %></p>

  </body>
</html>